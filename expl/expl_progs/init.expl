int main()
{
decl
  int temp, a, b;
enddecl

begin
  read(a);
  read(b);

  if (a < b) then
    temp = a;
    a = b;
    b = temp;
  endif;

  while (b != 0) do
    temp = a%b;
    a = b;
    b = temp;
  endwhile;

  write(a);
  return 0;
end
}
